#include<stdio.h>

/*int main()
{
    int a = 10;
    int* ptr =&a;

    printf("a = %d\n", a);      // diclar to the value of a
    printf("a = %d\n", *ptr);   // diclar to the value of a
    printf("a = %p\n", &a);     // diclar to the memory address of a
    printf("a = %p\n", ptr);    // diclar to the memory address of a
}
*/

int main()
{
    int a;

    printf("%d", sizeof(a));
}